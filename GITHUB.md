# Submission links

* GitHub repository: https://github.com/arcaneyears/daa-assignment1
* Branch: `main`
* Tag: `v1.0`

To publish this repository (the full git history with all feature branches and the v1.0 tag is
already inside this archive):

```bash
git remote add origin https://github.com/<your-user>/daa-assignment1.git
git push -u origin main
git push origin --all
git push origin --tags
```
